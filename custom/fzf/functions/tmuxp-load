# Get the session name
if [[ -n "$@" ]]; then
  session="$@"
else
  session="$(tmuxp ls | fzf-tmux)"
fi

# Load the session if it isn't already
if ! tmux has-session -t "$session" &>/dev/null; then
  tmuxp load -d "$session"
fi

# vim: ft=zsh
