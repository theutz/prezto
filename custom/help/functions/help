# vim: ft=zsh

if [[ -z "${argv[@]}" ]]; then
  print -l ${(k)helpers} | fzf | read helper
  $0 $helper
  return
fi

helper="$1"
cmd="${HELP_SOURCES[$helper]}"

if [[ -n "$cmd" ]]; then
  echo "Running: $cmd"
  eval "$cmd"
fi
