# vim: ft=zsh

linode-cli linodes ls --json |
  jq -r '.[] | [.id, .label, .status, .ipv4[0]] | @sh' |
  { echo "ID Label Status IP"; cat } |
  tr -d "'" |
  column -t |
  fzf --nth 2 --header-lines 1 --query="$1" --select-1 |
  awk '{ print $1 }'
